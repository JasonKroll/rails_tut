<% provide(:title, "Create user") %>
<h1>Sign up</h1>
<div>
  <%= form_for @user do |f| %>
    <%= render 'fields', f: f %>
    <%= f.submit "Create Account", class: "btn btn-large btn-primary" %>
  <% end %>
</div>
