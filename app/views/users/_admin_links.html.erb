[ <%= link_to "Delete", user, method: :delete, data: { confirm: "Are you sure?" } %>
<% if user.admin? %>
  | <%= link_to "Revoke admin", toggle_admin_user_path(user), remote: true %>
  <% else %>
  | <%= link_to "Give admin", toggle_admin_user_path(user), remote: true %>
<% end %> ]